# MA20 Screener — Python dependencies
# Stage 1: data infrastructure
pandas>=2.0.0
numpy>=1.24.0
pandas_market_calendars>=4.3.0
PyYAML>=6.0
pytz>=2023.3
requests>=2.31.0
# lxml is no longer needed by the pipeline: Phase A dropped the
# Wikipedia S&P 500 scrape when the universe moved to FMP's
# company-screener. Kept only because pandas.read_html still wants it if
# you ever add an HTML source back.
lxml>=4.9.0
# Neither yfinance, SEC EDGAR nor Stooq is used any more — every price
# and every market cap now comes from FMP. Intentionally not listed.
